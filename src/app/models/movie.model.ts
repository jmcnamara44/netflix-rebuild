export class Movie {
    constructor(public title: string, public rating: string, public genre: string[], public image: string) { }  //eventually the goal will be to add an image url to the constructor.
}
